import {PositiveNegativeNumberPipe} from './positive-negative-number.pipe';

describe('PositiveNegativeNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PositiveNegativeNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
